# Deliberately narrower than `pnpm validate`: build/test transitively depend on
# baseline-comparison.exp.integration.test.ts, which currently hangs indefinitely
# on a synchronous hot loop that no test timeout can interrupt (tracked in a
# GraphBox issue). typecheck+lint are the subset guaranteed to terminate today;
# widen this back to `pnpm validate` once that issue is fixed.
pnpm typecheck && pnpm lint
